*.jl.*.cov
*.jl.cov
*.jl.mem
*.rej
.DS_Store
.benchmarkci
Manifest.toml
benchmark/*.json
coverage
docs/build/
env
node_modules

# build artifacts (regenerable via deps/build.bat)
deps/build/
*.log

# OpenWolf local context (not shared)
.wolf/

# Claude Code local config (not shared)
.claude/
CLAUDE.md

# Local dev notes — NEVER track
XYPLOT_TODO.md
QTVTK_PLAN.md

# One-off / temporary code — NEVER track
src/isocs_shp.jl
